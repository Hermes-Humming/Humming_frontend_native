export default interface email {
  email: string;
}

export default interface signUp {
  email: string;
  password: string;
  nickname: string;
}

export default interface login {
  email: string;
  password: string;
}

export default interface changePW {
  password: string;
}
